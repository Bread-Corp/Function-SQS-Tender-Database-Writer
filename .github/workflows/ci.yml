name: CI for .NET Lambda (Main)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-lint-scan:
    name: Build, Lint, and Scan
    runs-on: windows-latest

    steps:
      - name: Set up JDK 17
        uses: actions/setup-java@99b8673ff64fbf99c8d329f525a97BC6286ab5f7
        with:
          java-version: 17
          distribution: 'zulu'

      - name: Checkout code
        uses: actions/checkout@a5ac7e51b4109f5124f2601c3e7b5f8e0539113f
        with:
          fetch-depth: 0  # Required for Sonar

      - name: Set up .NET 8
        uses: actions/setup-dotnet@B59B622E1A11F5EA95A591B1A736B461168B52B8
        with:
          dotnet-version: '8.0'

      - name: Install .NET linter
        run: dotnet tool install -g dotnet-format

      - name: Cache SonarQube Cloud packages
        uses: actions/cache@0c45773b623bea8c8e75f6c82b208c3cf94ea4f9
        with:
          path: ~\sonar\cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache SonarQube Cloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@0c45773b623bea8c8e75f6c82b208c3cf94ea4f9
        with:
          path: ${{ runner.temp }}\scanner
          key: ${{ runner.os }}-sonar-scanner
          restore-keys: ${{ runner.os }}-sonar-scanner

      - name: Install SonarQube Cloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          New-Item -Path ${{ runner.temp }}\scanner -ItemType Directory
          dotnet tool update dotnet-sonarscanner --tool-path ${{ runner.temp }}\scanner

      - name: SonarQube Begin Scan
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        shell: powershell
        run: |
          # Updated Sonar Project Key
          ${{ runner.temp }}\scanner\dotnet-sonarscanner begin /k:"Bread-Corp_Function-SQS-Tender-Database-Writer" /o:"bread-corp" /d:sonar.token="$env:SONAR_TOKEN"

      - name: Restore dependencies
        run: dotnet restore TenderDatabaseWriterLambda.sln

      - name: Build project
        run: dotnet build TenderDatabaseWriterLambda.sln --no-restore
      
      - name: Lint with .NET Format
        continue-on-error: true
        shell: powershell
        run: |
          # Updated solution file name and targeting only .cs files
          dotnet format TenderDatabaseWriterLambda.sln --verify-no-changes --include "**/*.cs"
        
      # --- TESTING: Uncomment this step when you add a test project ---
      # - name: Test with .NET Test
      #   continue-on-error: true 
      #   run: |
      #     dotnet test TenderDatabaseWriterLambda.sln --no-build --verbosity normal

      - name: SonarQube End Scan
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        shell: powershell
        run: |
          ${{ runner.temp }}\scanner\dotnet-sonarscanner end /d:sonar.token="$env:SONAR_TOKEN"
